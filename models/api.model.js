// const fs = require("fs");

// exports.fetchAllApiEndpoints = (cb) => {
//   fs.readFile("./endpoints.json", "utf8", (error, endPointsJSON) => {
//     if (error) cb({ status: 404, msg: "file not found" });
//     else cb(null, JSON.parse(endPointsJSON));
//   });
// };

// console.log(this.fetchAllApiEndpoints());
